{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.26.1",
  "scripts": {
    "build": "echo 'Pre-built dist files are committed to the repository. Use pnpm run build:full to rebuild locally.'",
    "build:full": "pnpm --filter @workspace/vitrine run build && pnpm --filter @workspace/api-server run build",
    "start": "NODE_ENV=production node app.js"
  }
}
